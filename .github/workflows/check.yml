name: Check profile source

on:
  pull_request:
    paths:
      - 'src/**'
      - 'test/**'
      - 'package*.json'
      - '.github/workflows/**'
  push:
    branches: [feature/profile-redesign]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: npm ci
      - run: npm test
